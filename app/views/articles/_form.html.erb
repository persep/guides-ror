<%= form_for @article do |f| %>
<%# @article is a resource corresponding to a full set of RESTful routes %>
<%# (resources :articles) and Rails is able to infer which URI and method to use%>

<%# form_for @article, url: articles_path do |f| %>
<%# the route has to be POST   /articles(.:format) articles#new %>

<%# form_for @article, url: article_path(@article), method: :patch do |f| %>
<%# the route has to be PATCH  /articles/:id(.:format) articles#update %>
<%# :id is a symbol that we use as a parameter %>

<% if @article.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@article.errors.count, "error") %> prohibited
    this article from being saved:</h2>
    <ul>
      <% @article.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.submit %>
  </p>
  <% end %>